"use client";

import { BlogItem } from "@/types";

type BlogCardProps = {
  item: BlogItem;
};

const BlogCard = (props: BlogCardProps): JSX.Element => {
  return <div></div>;
};

export default BlogCard;
